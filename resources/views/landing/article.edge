<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Hygeia</title>
  {{ style('app') }}
</head>
<body class="bg-hygeia-white font-display">
  @include('partials/header')

  {{--  BEGIN CONTENT  --}}
  <section class="items-center align-middle py-8">
    <div class="mx-5 md:mx-20">
      <h1 class="text-4xl font-semibold leading-tight my-6">Articles & News</h1>
        <div class="flex flex-col space-y-5">
          @each(article in articles)
            <div class="flex flex-col items-center md:items-start md:space-x-10 md:flex-row bg-white shadow-xl p-2 md:p-10 rounded-lg">
              <div class="items-center md:w-2/12">
                <img class="object-none object-center h-64 w-64" src="{{ article.url }}">
              </div>
              <div class="flex flex-col w-full overflow-x-auto md:w-10/12 space-y-3">
                <div class="text-xl md:text-2xl font-semibold">
                  <h1>{{ article.title }}</h1>
                </div>
                <p class="w-full overflow-ellipsis text-justify">
                  {{ article.article }}.
                </p>
              </div>
            </div>
          @endeach
        </div>
    </div>
  </section>
</body>
</html>

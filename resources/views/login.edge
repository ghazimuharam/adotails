<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Login</title>
  {{ style('app') }}
</head>
<body class="font-display">
  <div class="flex h-screen items-center align-middle justify-items-center md:space-x-5">

    <div class="hidden md:flex flex-col justify-center h-full w-2/5 bg-black">
      <h1 class="text-7xl text-center font-bold uppercase text-hygeia-pink">Hygeia</h1>
    </div>
    
    <div class="container w-full px-5 md:w-3/5 md:px-32">
      <div class="flex flex-col w-full">
        <h1 class="text-5xl">Sign In</h1> 
        <p class="py-5 text-xl text-gray-500">Sign in to continue to our application</p>
        @if(hasErrorFor('email'))
          <div class="bg-red-300 p-2 rounded-lg">
            {{ getErrorFor('email') }}
          </div>
        @endif
        @if(hasErrorFor('password'))
          <div class="bg-red-300 p-2 rounded-lg">
            {{ getErrorFor('password') }}
          </div>
        @endif
        <form action="{{ route('post.login') }}" method="POST" class="flex flex-col space-y-2">
          <div class="py-10">
            <div>
              <input class="p-1 py-3 border-b-2 focus:border-black focus:outline-none w-full" type="text" name="email" placeholder="Email" value="{{ old('email') }}" autocomplete="off">
            </div>
            <div>
              <input class="p-1 py-3 border-b-2 focus:border-black focus:outline-none w-full" type="password" name="password" placeholder="Password">
            </div>
          </div>
          {{ csrfField() }}
          <div>
            <button class="mt-10 w-full bg-hygeia-btn-black py-3 rounded-lg text-xl font-semibold text-hygeia-white" type="submit">Sign In</button>
          </div>
        </form>
      </div>
    </div>
  </div>
</body>
</html>
